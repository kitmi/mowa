'use strict';const Mowa=require('../server.js');const Util=require('../util.js');let passportAuthenticate=(opt,appModule)=>{if(!opt||!opt.strategy){throw new Mowa.Error.InvalidConfiguration('Missing strategy name.',appModule,'middlewares.passport.strategy')}let passport=appModule.getService('passport');return passport.authenticate(opt.strategy,opt.options)};passportAuthenticate.__metaMatchMethods=['post'];module.exports=passportAuthenticate;