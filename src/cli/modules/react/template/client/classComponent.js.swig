import React from 'react';
import PropTypes from 'prop-types';

import { appPath } from '../utils/mowa.js';


class {{ component }} extends React.Component {

    render() {

        return (
            <div>{{ component }} component</div>);
    }
}

{{ component }}.propTypes = {
};

export default {{ component }};